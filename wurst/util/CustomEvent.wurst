package CustomEvent

public module CustomEvent
    private trigger trig = CreateTrigger()

    function register(code func)
        trig.addCondition(Filter(func))
    
    function fire()
        trig.evaluate()

